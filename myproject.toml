[project]
name = "meta-evo-causal-discovery"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
  "scipy>=1.10",
  "networkx>=3.1",
  "matplotlib>=3.7",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["meta_evo_cd*"]